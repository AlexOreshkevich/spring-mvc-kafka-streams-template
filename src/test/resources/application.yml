spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: baeldung
message:
  topic:
    name: test-topic

monitor:
  kafka:
    bootstrap:
      config: "PLAINTEXT://localhost:9085"
    consumer:
      groupid: "baeldungGrp"